version: 0.2

# batch:
#   fast-fail: false | true
  # build-list:
  # build-matrix:
  # build-graph:

phases:
  # install:
    # run-as: Linux-user-name
    # on-failure: ABORT | CONTINUE
    # runtime-versions:
    #   nodejs: 12.x
    #   python: 3.8
    # commands:
    #   - command
    # finally:
    #   - command
    #   - command
  # pre_build:
  #   run-as: Linux-user-name
  #   on-failure: ABORT | CONTINUE
  #   commands:
  #     - command
  #     - command
  #   finally:
  #     - command
  #     - command
  build:
    # run-as: Linux-user-name
    # on-failure: ABORT | CONTINUE
    commands:
      - echo "packaging piotflow"
    # finally:
    #   - command
    #   - command
  # post_build:
    # run-as: Linux-user-name
    # on-failure: ABORT | CONTINUE
    # commands:
      # - command
  #   finally:
  #     - command
  #     - command
# reports:
#   report-group-name-or-arn:
#     files:
#       - location
#       - location
#     base-directory: location
#     discard-paths: no | yes
#     file-format: report-format
artifacts:
  files:
    - piotflow.py
    - flowmeter.py
  name: 1.1/piotflow.zip
#   discard-paths: no | yes
#   base-directory: location
#   exclude-paths: excluded paths
#   enable-symlinks: no | yes
#   s3-prefix: prefix
#   secondary-artifacts:
#     artifactIdentifier:
#       files:
#         - location
#         - location
#       name: secondary-artifact-name
#       discard-paths: no | yes
#       base-directory: location
#     artifactIdentifier:
#       files:
#         - location
#         - location
#       discard-paths: no | yes
#       base-directory: location
# cache:
#   paths:
#     - path
#     - path
  #   finally:
  #     - command
  #     - command
